import React from 'react';
import Button from './Button/Button'

const Buttons = (props) => {
    return (
        <div>
          <Button></Button>  
        </div>
    );
}

export default Buttons;
